<section class="hero">

  <img 
    src="/logo_airsoft_nation.svg" 
    alt="Airsoft Nation"
    class="logo"
  />

  <p class="tagline">
    Comunidad, juego y evolución táctica
  </p>

</section>

<style>
.hero {
  text-align: center;
  padding: 60px 20px 40px;
}

/* Logo visible y con profundidad real */
.logo {
  width: 260px;
  max-width: 85%;
  filter:
    brightness(1.25)
    contrast(1.1)
    drop-shadow(0 0 20px rgba(0,180,255,0.35))
    drop-shadow(0 10px 40px rgba(0,0,0,0.6));
  animation: fadeIn 0.8s ease forwards;
  opacity: 0;
}

/* Subtítulo */
.tagline {
  margin-top: 18px;
  font-size: 0.95rem;
  opacity: 0.75;
  letter-spacing: 0.5px;
  animation: fadeIn 1.2s ease forwards;
  opacity: 0;
}

@keyframes fadeIn {
  to { opacity: 1; }
}
</style>
